import React from "react";
import { Alert } from "react-bootstrap";

function Orders() {
   return (
      <>
         <Alert variant="info">
            Esta seccion se encuentra en construccion
         </Alert>
      </>
   );
};

export default Orders;