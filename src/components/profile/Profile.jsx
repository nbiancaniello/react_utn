import { Alert } from "react-bootstrap";

function Profile() {
   return (
      <>
         <Alert variant="info">
            Esta seccion se encuentra en construccion
         </Alert>
      </>
   );
}

export default Profile;